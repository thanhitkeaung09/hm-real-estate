import React from "react";
import ForgetPassword from "../../component/Password/Change";

const Change = ({selectedLanguage}) => {
  return (
    <div>
      <ForgetPassword selectedLanguage={selectedLanguage} />
    </div>
  );
};

export default Change;
